# Caches
.ruff-cache/
__pycache__/
*.pyc
*.py[cod]
*$py.class


# Editor / tooling
.claude/

# Backup & temp files
*.bak
*.swp
*~

# Secrets — never commit a password CSV (see utils/pwsearch.py)
*.csv

# Calendario: config, segreti e virtualenv generato (vedi *.example)
waybar/calendar.conf
waybar/calendar.env
waybar/.calvenv/

# Node (cache prettier / installazioni locali)
node_modules/
package-lock.json
openrouter/
nvim/lazy.nvim/

# File temporanei e di swap degli editor (Neovim)
*.swp
*.swo
*~
*.bak
.netrwhist

# Credenziali e database password (protezione per pwsearch)
*.kdbx
*.key
*.pem
id_rsa*
.env
secrets/
.auth

# File generati a runtime o log
*.log
.DS_Store
