# Caches
.ruff-cache/
__pycache__/
*.pyc

# Editor / tooling
.claude/

# Backup & temp files
*.bak
*.swp
*~

# Secrets — never commit a password CSV (see utils/pwsearch.py)
*.csv

# Calendario: config, segreti e virtualenv generato (vedi *.example)
waybar-niri/calendar.conf
waybar-niri/calendar.env
waybar-niri/.calvenv/

# Node (cache prettier / installazioni locali)
node_modules/
package-lock.json
