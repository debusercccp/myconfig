# Caches
.ruff-cache/
__pycache__/
*.pyc

# Editor / tooling
.claude/

# Backup & temp files
*.bak
*.swp
*~

# Secrets — never commit a password CSV (see utils/pwsearch.py)
*.csv

# Calendario: URL segreto iCal e virtualenv generato (vedi calendar.conf.example)
waybar-niri/calendar.conf
waybar-niri/.calvenv/
