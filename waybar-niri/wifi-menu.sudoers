# Permette a noya di usare wpa_cli per gestire il WiFi senza password sudo.
# Installa con: sudo install -m 440 wifi-menu.sudoers /etc/sudoers.d/wifi-menu
noya ALL=(ALL) NOPASSWD: /usr/sbin/wpa_cli -p /run/wpa_supplicant -i wlp4s0 *
